# Copy this file to .env.local and fill real secrets.
# .env.local is ignored by git.

CLOUDBASE_ENV_ID=cloud1-8g5sii8ve777802e
CLOUDBASE_SECRET_ID=your_tencent_cloud_secret_id
CLOUDBASE_SECRET_KEY=your_tencent_cloud_secret_key

ADMIN_OPERATOR_KEY=your_billing_internal_operator_key
ADMIN_OPERATOR_ID=admin_console

ADMIN_USERNAME=admin
# Generate a PBKDF2 hash:
# node -e "const crypto=require('crypto');const p=process.argv[1];const s=crypto.randomBytes(16).toString('hex');const i=100000;const h=crypto.pbkdf2Sync(p,Buffer.from(s,'hex'),i,32,'sha256').toString('hex');console.log('pbkdf2sha256:'+i+':'+s+':'+h)" "your-password"
ADMIN_PASSWORD_HASH=pbkdf2sha256:100000:replace_salt_hex:replace_hash_hex
ADMIN_SESSION_SECRET=replace_with_a_long_random_string
ADMIN_SESSION_TTL_SECONDS=28800

ADMIN_WEB_BRIDGE_HOST=127.0.0.1
ADMIN_WEB_BRIDGE_PORT=8788
ADMIN_WEB_BRIDGE_TIMEOUT_MS=15000
